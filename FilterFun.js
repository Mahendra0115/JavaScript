// filter -> it is use to filter element in array. 
  // like -> access  even/odd num in array elements.

  let arr1 = [45,37,50,95,70,60];
  let arr2 = arr1.filter((val)=>{
      if(val > 90){
         return val;
      }
  }) 
//   console.log(5,6);